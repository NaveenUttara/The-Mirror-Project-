# Mirror Backend Application

The Medusa application is configured in `medusa-config.ts`. Mirror-specific data models, services, and migrations are located in `src/modules/mirror`.

Use the parent workspace commands documented in `../../README.md`. Keep `.env` private and use `.env.template` only as a list of required variables.
